<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gss.web.common.mapper.BossMapper">
	<insert id="insertBoss" parameterType="com.gss.web.common.domain.Boss">
		INSERT INTO bosstab ( 
			bossname, 
			bossimagepath,
			bossgrade
		) values(
			#{bossName},
			#{bossImagepath},
			#{bossGrade}
		)
	</insert>
	<select id="selectByBoss" parameterType="Map" resultType="int">
		select
			bossnum,
			bossname,
			bossimagepath,
			bossgrade
		from bosstab
		where bossname=#{bossName} and bossgrade=#{bossGrade}
	</select>
</mapper>