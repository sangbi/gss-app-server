<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gss.web.common.mapper.BossMapper">
	<insert id="insertBoss" parameterType="com.gss.web.common.domain.Boss">
		INSERT INTO bosstab ( 
			bossname, 
			bossimagepath,
			bossgrade
		) values(
			#{bossName},
			#{bossImagepath},
			#{bossGrade}
		)
	</insert>
	
	<select id="selectByBoss" parameterType="Map" resultType="int">
		select
			count(*)
		from
			bosstab
		where 
			bossname=#{bossName} and bossgrade=#{bossGrade}
	</select>
	
	<select id="selectAllBoss" parameterType="com.gss.web.common.domain.Boss" resultType="com.gss.web.common.domain.Boss">
		select
			bossname, 
			bossimagepath,
			bossgrade
		from 
			bosstab
		
	</select>
	
	<select id="selectBossCount"  resultType="int">
		select
			count(*)
		from
			bosstab
	</select>
	
	
	<select id="selectAllBossPaging" parameterType="int" resultType="com.gss.web.common.domain.Boss">
		select
			bossname, 
			bossimagepath,
			bossgrade
		from 
			bosstab
		LIMIT 7
			OFFSET (#{page} - 1) * 7
	</select>
	
	
	<delete id="deleteByBossName" parameterType="Map">
		delete
		from 
			bosstab
		where 
			bossname=#{bossName} and bossgrade = #{bossGrade}
	</delete>
	
	<select id="selectByBossNameAndGrade" parameterType="Map" resultType="com.gss.web.common.domain.Boss">
		select
			bossname, 
			bossimagepath,
			bossgrade
		from
			bosstab
		where 
			bossname=#{bossName} and bossgrade = #{bossGrade}
	</select>
</mapper>