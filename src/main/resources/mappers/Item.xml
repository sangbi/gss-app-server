<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gss.web.common.mapper.ItemMapper">
	<insert id="insertItem" parameterType="com.gss.web.common.domain.Item">
		INSERT INTO itemtab ( 
			classification, 
			itemimagepath,
			itemname
		) values(
			#{classification},
			#{itemImagepath},
			#{itemName}
		)
	</insert>
	<select id="selectByItem" parameterType="String" resultType="com.gss.web.common.mapper.ItemMapper">
		select
			itemnum,
			classifiction,
			itemimagepath,
			itemname
		from itemtab
		where itemname=#{itemName}
	</select>
</mapper>