<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gss.web.common.mapper.ItemMapper">
	<insert id="insertItem" parameterType="com.gss.web.common.domain.Item">
		INSERT INTO itemtab ( 
			classification, 
			itemimagepath,
			itemname
		) values(
			#{classification},
			#{itemImagepath},
			#{itemName}
		)
	</insert>
	<select id="selectByItem" parameterType="String" resultType="com.gss.web.common.mapper.ItemMapper">
		select
			itemnum,
			classification,
			itemimagepath,
			itemname
		from itemtab
		where itemname=#{itemName}
	</select>
	<select id="selectSearchItem" parameterType="com.gss.web.api.dto.ItemDto" resultType="com.gss.web.api.dto.ItemDto">
		select
			classification,
			itemname
		from itemtab
		where delete_yn="N"
		<choose>
			<when test="type !=null and type.equals('classification')">
			and classification LIKE CONCAT('%', #{keyword}, '%')
			</when>
			<when test="itemname !=null and type.equals('itemname')">
			and itemname LIKE CONCAT('%', #{keyword}, '%')
			</when>
		</choose>
	</select>
</mapper>